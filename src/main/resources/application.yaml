ktor:
    application:
        modules:
            - com.debdevs.ApplicationKt.module
    deployment:
        port: 8080
    database:
        db_user: $NEUMANN_DB_USERNAME
        db_password: $NEUMANN_DB_PASSWORD